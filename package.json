{
  "name": "velo",
  "version": "0.1.0",
  "description": "Radsport Director – a data-driven cycling manager game",
  "main": "dist/main/index.js",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "concurrently \"npm --prefix backend run dev\" \"wait-on http://localhost:3101/health && npm --prefix frontend run dev -- --host 0.0.0.0\"",
    "dev": "npm run start",
    "browser": "npm run start",
    "test": "npm --prefix backend run test",
    "calibrate:reference": "node tools/quicksim-calibration/run.js",
    "calibrate:aggregate": "node tools/quicksim-calibration/runAggregate.js",
    "calibrate:groups": "node tools/quicksim-calibration/runGroups.js",
    "calibrate:validate": "node tools/quicksim-calibration/runValidate.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
